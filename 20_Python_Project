pets = {}
#Запрашиваем имя питомца:
name = input("Кличка вашего питомца?")
# Запрашивает вид питомца. 
view = input("Кем ялвяется ваш питомец?")

# Запрашиваем возраст питомца:
age = int(input("Сколько лет вашему питомцу?"))

# Зарпашивает имя владельца питомца.
owner = input("Имя владельца:")
 # Записывае ввидённые данные в словарь через внешний ключ.
 # В роли ключи злужит перменная name.
pets[name] = {
    "Кличка": name,
    "Вид":view,
    "Возраст": age,
    "Владелец": owner
}

print(pts[name])